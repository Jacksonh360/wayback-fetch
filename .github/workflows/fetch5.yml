name: wayback-fetch5
on: { workflow_dispatch: {} }
jobs:
  harvest:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - uses: actions/checkout@v4
      - run: ./fetch5.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: captures5, path: out5/ }
