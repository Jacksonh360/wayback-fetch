name: wayback-fetch3
on: { workflow_dispatch: {} }
jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - run: ./fetch3.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: captures3, path: out3/ }
